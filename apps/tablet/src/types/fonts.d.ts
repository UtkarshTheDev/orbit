declare module '@fontsource/*' {
  // Fontsource packages are side-effect imports for CSS
  // They don't export meaningful JavaScript values
}

declare module '@fontsource-variable/*' {
  // Fontsource variable packages are side-effect imports for CSS
  // They don't export meaningful JavaScript values
}
