# Backend Server Configuration

# Server port (Render will set this automatically in production)
PORT=3001

# Node environment
NODE_ENV=development

# Allowed origins for CORS and WebSocket connections (comma-separated)
# Add your production frontend domain here
WS_ALLOWED_ORIGINS=https://orbit-robo.vercel.app,http://localhost:5173

# To use, copy this file to .env and adjust values as needed for your deployment environment.
# For Render deployment:
# - PORT will be automatically set by Render
# - Set WS_ALLOWED_ORIGINS to include your Vercel frontend domain
# - Set NODE_ENV=production