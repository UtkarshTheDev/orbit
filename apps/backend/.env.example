# Backend WebSocket configuration example

# WebSocket server host
# Use 0.0.0.0 for local development or your production backend domain
WS_HOST=0.0.0.0  # or https://backend-domain.com

# Port number for the WebSocket server
WS_PORT=3001

# Allowed origins for WebSocket connections (comma-separated)
WS_ALLOWED_ORIGINS=https://orbit-robot.vercel.app,http://localhost:5173

# To use, copy this file to .env and adjust values as needed for your deployment environment.